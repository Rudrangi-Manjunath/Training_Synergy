package Secondpackage;

import static java.lang.Math.sqrt;

public class staticimport {
    public static void main(String[] args) {
        System.out.println(sqrt(4));   // if we want to use multiple we can import static such that we can reduce code writing
        //  Ex. Instead of Math.sqrt() we can use sqrt()

    }
}
